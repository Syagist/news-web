import React from 'react';
import {StyledNotFound} from "./StyledNotFound";

const NotFound = () => {
    return (
        <StyledNotFound>
            Not Found
        </StyledNotFound>
    );
};

export default NotFound;