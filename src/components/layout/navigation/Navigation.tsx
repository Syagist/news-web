import React from 'react';
import {StyledNavigation} from "./StyledNavigation";

const Navigation = () => {
    return (
        <StyledNavigation>
            nav
        </StyledNavigation>
    );
};

export default Navigation;